// import { google } from 'googleapis';
// import { config } from '../config';

// export const clientId = config.google.clientId;
// export const clientSecret = config.google.clientSecret;
// export const redirectUri = config.google.redirectUrl;
// export const scope = config.google.scopes;

// export const oAuth2Client: any = new google.auth.OAuth2(clientId, clientSecret, redirectUri);
